export enum ArrowDirection {
  Up = "ArrowUp",
  Down = "ArrowDown",
  Right = "ArrowRight",
  Left = "ArrowLeft",
}

export enum RobotTextCommand {
  UP = "N",
  Down = "S",
  Right = "Ö",
  Left = "V",
}

export type Coordinate = [number, number];
