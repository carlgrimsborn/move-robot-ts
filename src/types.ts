export enum Direction {
  Up = "ArrowUp",
  Down = "ArrowDown",
  Right = "ArrowRight",
  Left = "ArrowLeft",
}

export enum RobotCommand {
  UP = "U",
  Down = "D",
  Right = "R",
  Left = "L",
}

export type Coordinate = [number, number];